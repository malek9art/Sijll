# سجل التغييرات — سِجِلّ (Sijll)

## [غير منشور] — 2026-08-04

### 🔐 أساس الحسابات الخاصة
- إضافة عميل Neon Auth لحسابات البريد الإلكتروني مع بوابة مصادقة قبل فتح التطبيق.
- إنشاء قاعدة IndexedDB مستقلة مشتقة من `user_id` لكل حساب بدلاً من قاعدة مشتركة.
- إضافة تسجيل خروج يعزل قاعدة المستخدم الحالية وتسجيل منفذ العمليات في سجل التدقيق.
- إيقاف زرع البيانات الشخصية الافتراضية؛ الحساب الجديد يبدأ بالقوالب ودليل الحسابات العام فقط.
- إضافة مخطط Neon للمرحلة الأولى في `docs/neon-phase1.sql`.

### ☁️ النسخ المشفرة اليدوية
- تشفير النسخ تلقائياً بمفتاح حساب Neon قبل تنزيلها أو رفعها إلى Google Drive.
- إضافة تحقق من مالك النسخة وإصدارها قبل الاستعادة.
- تعطيل الرفع التلقائي؛ النسخ تتم بطلب المستخدم فقط.

## [1.1.0] — 2026-08-03

### ✨ المزايا الجديدة
- **إلحاق التوقيع وبصمة الإصبع بالمستندات** عبر حساس البصمة (WebAuthn): توثيق بيومتري مربوط ببصمة المستند الرقمية مع ختم زمني، وشارة «موثق بيومترياً» في الطباعة — البصمة لا تُخزَّن ولا تغادر الحساس
- **رسم بصمة الإبهام داخل المستند**: تُرسم بصمة واقعية (نقوش حلقية، انكسارات، لطخات حبر) في صندوق توقيع الموقّع كأنه بَصَّم على ورقة خارجية — ثابتة وفريدة لكل موقّع (مولّد حتمي من الاعتماد البيومتري) وتظهر في الطباعة وPDF
- **رمز QR بصفحة تحقق مستقلة** (`verify.html`): المسح يفتح صفحة خارج النظام تعرض بيانات المستند وتؤكد مصداقيته عبر بصمة رقمية SHA-256 مضمّنة في الرمز، وتكشف أي تلاعب
- **تجويد القوالب القانونية الثمانية**: صياغة أدق، بسملة، رقم مستند، وشهود بهوياتهم في كل القوالب (مع حقلي رقم هوية للشهود في المحرر)
- **محتوى المستندات بخط النظام IBM Plex Sans Arabic** (مطابقة كاملة للتطبيق)
- **نسخ احتياطية محلية تلقائية** داخل الجهاز عند كل تشغيل مع سياسة احتفاظ
- **رفع يومي تلقائي إلى Google Drive** مع حذف تلقائي للنسخ الأقدم (احتفاظ محدود قابل للضبط)
- **حذف تلقائي للبيانات** بسياسة احتفاظ (سجل التدقيق والتنبيهات بالأيام، العمليات الملغاة بالأشهر) تُنفَّذ عند التشغيل ويدوياً من الإعدادات

### 🔧 إصلاحات النسخ الاحتياطي
- استبدال جدول الإعدادات والعدادات عند الاستعادة — لا مزيد من أرقام مستندات مكررة بعد الاستعادة
- استبدال الجداول الفارغة (الحسابات/القوالب) بدل إبقاء القديمة
- التحقق من سلامة بنية النسخة قبل الاستيراد مع رسائل خطأ واضحة
- عرض «آخر نسخة احتياطية تلقائية» الحقيقية بدل التاريخ الوهمي

### 🧪 الاختبارات
- 71 اختباراً آلياً (20 اختباراً جديداً: بصمة المستند، رسم البصمة، الاستيراد، التنظيف التلقائي، النسخ المحلية)

## [1.0.0] — 2026-08-03

### ✨ المزايا
- منصة عربية احترافية لإدارة الحسابات والمديونيات
- كشوف حساب موحدة بثمانية أعمدة
- محاسبة مزدوجة القيد مع تحقق التوازن
- 8 قوالب مستندات قانونية احترافية
- طباعة A4 احترافية مع خطوط عربية
- تصدير PDF متعدد الصفحات
- مشاركة عبر واتساب (Web Share API)
- قفل PIN + بصمة (WebAuthn)
- نسخ احتياطي مشفر AES-256
- نسخ احتياطي على Google Drive
- عمل كامل دون اتصال (PWA + IndexedDB)
- وضع داكن/فاتح
- 19 اختبار آلي

### 🔒 الأمان
- تشفير PIN بـ PBKDF2-SHA256 (100,000 تكرار)
- Rate limiting تصاعدي على محاولات PIN
- Content Security Policy (CSP)
- Error Boundary شامل
- تأكيد مزدوج للحذف

### ⚡ الأداء
- Code splitting + Lazy loading لكل الصفحات
- الحزمة الأساسية: 131 KB (gzip)
- أيقونات PWA محسّنة (96 KB + 19 KB + 89 KB)
- Debounce للبحث (300ms)
- Dashboard مُحسّن (aggregate queries)
